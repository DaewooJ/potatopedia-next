export const metadata = {
  title: { absolute: "My Library — Potatopedia" },
  description: "Your saved potato research questions and answers.",
};

export default function LibraryLayout({ children }) {
  return children;
}
